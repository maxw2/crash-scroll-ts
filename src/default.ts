import { OPTIONS, POS } from './interface'

let _default: OPTIONS = {
    scrollX: false,

}

let pos: POS = {
    downX: 0,
    downY: 0,
    moveX: 0,
    moveY: 0,
    disX: 0,
    disY: 0,
    x: 0,
    y: 0
}

export { _default, pos }
